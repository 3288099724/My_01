package com.bs.repository;/*
package com.bs.repository;

import com.bs.entity.Supplier;
import org.springframework.data.domain.Pageable;
import org.springframework.data.elasticsearch.repository.ElasticsearchRepository;

import java.util.List;

public interface SupplierRepository extends ElasticsearchRepository<Supplier,String> {


    //自定义查询
    List<Supplier> findByNameLike(String name, Pageable pageable);

    List<Supplier> findByLeaderLike(String leader, Pageable pageable);
}
*/
